def version() -> str:
    """Retorna a versão da engine compilada em Rust."""

def to_parquet(src: str, dst: str, kind: str | None = None) -> int:
    """Converte um arquivo CSV do CNPJ (empresas, socios ou estabelecimentos) para Parquet em disco."""
