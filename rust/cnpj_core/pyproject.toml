[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "cnpj_core"
requires-python = ">=3.14"
dynamic = ["version"]

[tool.maturin]
features = ["pyo3/extension-module"]
